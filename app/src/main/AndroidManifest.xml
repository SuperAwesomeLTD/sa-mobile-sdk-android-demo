<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="aademo.superawesome.tv.awesomeadsdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme">
        <activity
            android:name=".activities.main.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.settings.SettingsActivity"
            android:label="@string/page_settings_title"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".activities.display.DisplayActivity"
            android:label="@string/page_display_title"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".activities.creatives.CreativesActivity"
            android:label="@string/page_creatives_title"
            android:screenOrientation="portrait"/>
    </application>

</manifest>