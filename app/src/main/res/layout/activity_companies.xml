<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    tools:context=".activities.companies.CompaniesActivity">

    <android.support.design.widget.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/MyMaterialTheme.Base">

        <android.support.v7.widget.Toolbar
            android:id="@+id/CompaniesToolbar"
            android:layout_width="match_parent"
            app:titleTextAppearance="@style/AppTheme.Toolbar.Title"
            style="@style/SmallToolBarStyle"
            android:layout_height="?attr/actionBarSize">
        </android.support.v7.widget.Toolbar>

    </android.support.design.widget.AppBarLayout>

    <EditText
        android:layout_margin="20dp"
        android:id="@+id/CompanySearch"
        android:layout_width="match_parent"
        android:hint="@string/page_companies_search_placeholder"
        android:inputType="text"
        android:layout_height="40dp" />

    <ListView
        android:id="@+id/CompaniesListView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="none"
        android:dividerHeight="0dp"
        android:divider="@null"/>

</LinearLayout>
