dependencies:
  pre:
    - echo y | android update sdk --no-ui --all --filter tools,android-24,build-tools-24.0.0

checkout:
  post:
    - cp local.properties.ci local.properties

test:
  override:
    - ./gradlew clean
    - ./gradlew build
